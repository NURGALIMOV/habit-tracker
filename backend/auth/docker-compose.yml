version: '3.7'
services:
  auth:
    networks:
      - habit-tracker-backend
    container_name: auth
    image: auth
    build: .
    depends_on:
      - auth_db
  auth_db:
    container_name: auth-db
    image: postgres
    restart: on-failure
    networks:
      - habit-tracker-backend
    environment:
      POSTGRES_DB: authdb
      POSTGRES_USER: auth
      POSTGRES_PASSWORD: auth
networks:
  habit-tracker-backend:
    name: habit-tracker-backend
